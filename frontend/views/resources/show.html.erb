<%= setup_context :object => @resource, :title => "#{resource_display_string(@resource.title,@resource.id_0)}" %>

<%= render_aspace_partial :partial => "transfer/transfer_failures", :locals => { :transfer_errors => @transfer_errors } %>
<%= render_aspace_partial :partial => "shared/largetree", :locals => {:root_record => @resource, :read_only => true} %>
